services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_USER=parsewell
      - POSTGRES_PASSWORD=parsewell
      - POSTGRES_DB=parsewell
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
